# Karabiner-Elements is installed via Homebrew cask (homebrew.nix) and
# manages its own launchd agents. The nix-darwin service module is not
# used because the app comes from Homebrew, not nixpkgs.
{...}: {}
